# Port targets — `ratatui-macros-kotlin` (driven by codex-rs demand)

> Generated by `scaffold/generate_port_targets.py` from `codex_external_usage.json`. Do not hand-edit. Re-run after rebuilding the usage data.

This is the **minimum subset** of `ratatui-macros` that codex-rs uses, and therefore the minimum subset that `ratatui-macros-kotlin` needs to port in order for codex-kotlin to compile and run. Symbols are sorted by codex demand (most-used first). Anything not listed here is not currently required by codex — defer it.

**Jira Initiative**: [COD-115] (rank `265`, tier `direct`, status `To Do`)

**Cargo package name**: `ratatui-macros`
**Rust module name (in `use` statements)**: `ratatui_macros`

## Summary

- **2** distinct symbols imported by codex-rs
- **4** total `use` statements
- **2** codex source files affected
- **1** codex member crates transitively depend on this

## Granularity caveat

This list is **import-level**, not call-site-level. A symbol appearing here means codex `use`'d it; the specific functions, methods, fields, or trait impls codex calls on the imported name are not enumerated. For an initial port, implement the symbol's full API as defined in upstream Rust. For a tighter minimum, drill into each codex source file listed below and grep for the imported name to find concrete invocations — the graph tells you exactly which files to grep.

## Symbols by codex demand (port these in order)

### `ratatui_macros::line`

- **3 `use` site(s)** across **1 codex member crate(s)**: `codex-tui`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `tui/src/chatwidget.rs:8761` (`codex-tui`) — `use ratatui_macros::line`
- `tui/src/chatwidget.rs:8891` (`codex-tui`) — `use ratatui_macros::line`
- `tui/src/history_cell.rs:582` (`codex-tui`) — `use ratatui_macros::line`

### `ratatui_macros::text`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-tui`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `tui/src/history_cell.rs:583` (`codex-tui`) — `use ratatui_macros::text`
